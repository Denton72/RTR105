def computepay(hrs,rate):
 if hrs > 40.0:
  p = rate * 40
  p = p+(1.5*rate*(hrs-40))
 else:
  p=rate*hrs
 return p 
hrs = float(input("Enter Hours:"))
rate = float(input("Enter Rate:"))
print (computepay(hrs,rate))


mistakes:
print computepays,rate)
  p = p(1.5*rate*(rate-4))

largest = None
smallest = None
while True:
    inp = input("Enter a number: ")
    if inp == "done" : break
    try:
        num = int(inp)
        
        if smallest is None:
         smallest = num
         largest = num
        elif num > largest:
         largest = num
    except ValueError:
        print ("Invalid input")
        continue 
print("Maximum is", largest)
print("Minimum is", smallest)
